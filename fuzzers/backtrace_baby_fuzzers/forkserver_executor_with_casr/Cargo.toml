[package]
name = "forkserver_executor_with_casr"
version = "0.0.1"
edition = "2021"

[features]
casr = []

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
opt-level = 3

[dependencies]
libafl = { path = "../../../libafl/", features = ["casr"]}
