[package]
name = "libafl_tinyinst"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html


# memo
# fixing mbuild in TinyInst to build it on visual studio 2022 first?
# include xed.lib dbghelp.lib as deps?
# find a way to stop manually generate bridge files
# use x64 native tools cmd prompt
[dependencies]
cxx = "1.0"
libafl = { path = "../libafl", version = "0.8.0", features = [
    "std",
    "libafl_derive",
] }
tinyinst-rs = { git = "https://github.com/elbiazo/tinyinst-rs" }

[build-dependencies]
cmake = "0.1"
