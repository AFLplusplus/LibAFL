[package]
name = "libafl_tinyinst"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html



# memo
# fixing mbuild in TinyInst to build it on visual studio 2022 first?
# include xed.lib dbghelp.lib as deps?
# find a way to stop manually generating bridge files
# use x64 native tools cmd prompt
[dependencies]
cxx = "1.0"

[build-dependencies]
cmake = { git = "https://github.com/tokatoka/cmake-rs" } # use crates.io's one when the latest one gets published.