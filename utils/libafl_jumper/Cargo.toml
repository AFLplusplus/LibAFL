[package]
name = "libafl_jumper"
edition = "2021"
version.workspace = true

[features]
default=["std"]
std=[]

[build-dependencies]
cc = "1"

[dependencies]
hex = { version = "0.4", default-features = false }
