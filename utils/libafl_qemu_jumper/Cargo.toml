[package]
name = "libafl_qemu_jumper"
edition = "2021"
version.workspace = true

[features]
default=["std"]
std=[]

[build-dependencies]
cc = "1"

[dependencies]
hex = { version = "0.4", default-features = false }

[profile.release]
inherits = "release"
panic = "abort"
debug = true
